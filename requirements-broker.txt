# Fubon broker API (Windows-only, used by fubon_client.py for holdings query)
#
# fubon_neo is not on public PyPI and its official wheel is win_amd64-only,
# so it can't install on GitHub Actions (Linux) and is kept out of
# requirements.txt. Install it manually from the wheel Fubon provides, then:
#     py -3.12 -m pip install .\fubon_neo-2.2.8-cp37-abi3-win_amd64.whl
#     py -3.12 -m pip install -r requirements-broker.txt
#
# Supports Python 3.8-3.13, not 3.14.
python-dotenv
